# mela_example
Mela library example package

# Install

To install run this command `mpremote mip install `

# Usage

Example of usage:

```

from mela import Mela
plc=Mela()
print(plc.info.free)

```
